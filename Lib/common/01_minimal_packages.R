suppressMessages(
  do.call(function() {
    library(readr)
    library(yaml)
    library(tidyr)
    library(dplyr)
    library(purrr)
    library(magrittr)
    library(stringr)
    library(lubridate)
    library(ROracle)}, list()))
